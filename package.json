{
  "name": "school-payment",
  "version": "1.0.0",
  "description": "School Payment Advisor - Lean4 verified payment strategy",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n api,frontend -c blue,green \"npm run dev:api\" \"npm run dev:frontend\"",
    "dev:api": "cd api-server && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build:lean": "cd lean-backend && ~/.elan/bin/lake build",
    "test:api": "curl -s http://localhost:3001/health && curl -s -X POST http://localhost:3001/rpc -H 'Content-Type: application/json' -d '{\"jsonrpc\":\"2.0\",\"method\":\"ping\",\"params\":{},\"id\":1}'",
    "clean": "pkill -f 'tsx.*api-server' || true; pkill -f 'lake exe advisor' || true; pkill -f 'vite' || true",
    "restart": "npm run clean && sleep 2 && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
